goog.provide('ga_editglstyle_controller');

(function() {

  var module = angular.module('ga_editglstyle_controller', [
    'pascalprecht.translate'
  ]);

  module.controller('GaEditGlStyleController', function($scope) {
    // var options = {};

    // We use options provided by parent controller.
    // $scope.options = angular.extend(options, $scope.options || {});
  });
})();
