#!/bin/bash

echo "
source src_address
{
    type = pgsql
    sql_host = $1 
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , strname1||' '||deinr||' <b>'||plz||' '||ort_27||'</b>' as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'address'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_parcel
{
    type = pgsql
    sql_host = $1
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , gemname||' '||name as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'parcel'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_sn25
{
    type = pgsql
    sql_host = $1
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , '<b>'||name||'</b> ('||kanton||') - '||gemname as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'sn25'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_gg25
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , '<b>'||gemname||' ('||kanton||')</b>' as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'gg25'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_kantone
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , remove_accents(search_name) as detail \
        , '<b>'||name||'</b>' as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'kantone'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_district
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , '<b>'||name||'</b>' as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'district'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

source src_zipcode
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = search
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
        gid as id \
        , search_name as detail \
        , '<b>'||plz||' - '||ort_27||' ('||kanton||')</b>' as label \
        , origin as origin \
        , quadindex(the_geom) as geom_quadindex \
        , st_box2d(the_geom) as geom_st_box2d \
        , rank as rank \
        , gid as id \
        from swiss_search \
        where origin = 'zipcode'
    sql_attr_bigint = id
    sql_attr_uint = rank
    sql_attr_string = label
    sql_attr_string = origin
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}


source src_layers_de
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = bod
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
            bgdi_id::bigint as id \
            , '<b>'||kurzbezeichnung||'</b>' as label \
            , 'layer' as origin \
            , volltextsuche as detail \
            , bod_layer_id as layer \
            , 'de' as lang \
            , projekte as topics \
            , bgdi_id::bigint as id \
        from view_bod_layer_info_de
    sql_attr_bigint = id
    sql_attr_string = lang
    sql_attr_string = origin
    sql_attr_string = label
    sql_field_string = topics
    sql_field_string = layer
    sql_field_string = detail
}

source src_layers_fr
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = bod
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
            bgdi_id::bigint as id \
            , '<b>'||kurzbezeichnung||'</b>' as label \
            , 'layer' as origin \
            , volltextsuche as detail \
            , bod_layer_id as layer \
            , 'fr' as lang \
            , projekte as topics \
            , bgdi_id::bigint as id \
        from view_bod_layer_info_fr
    sql_attr_bigint = id
    sql_attr_string = lang
    sql_attr_string = origin
    sql_attr_string = label
    sql_field_string = topics
    sql_field_string = layer
    sql_field_string = detail
}

source src_layers_en
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = bod
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
            bgdi_id::bigint as id \
            , '<b>'||kurzbezeichnung||'</b>' as label \
            , 'layer' as origin \
            , volltextsuche as detail \
            , bod_layer_id as layer \
            , 'en' as lang \
            , projekte as topics \
            , bgdi_id::bigint as id \
        from view_bod_layer_info_en
    sql_attr_bigint = id
    sql_attr_string = lang
    sql_attr_string = origin
    sql_attr_string = label
    sql_field_string = topics
    sql_field_string = layer
    sql_field_string = detail
}

source src_layers_it
{
    type = pgsql
    sql_host = ec2-46-137-49-117.eu-west-1.compute.amazonaws.com
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = bod
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
            bgdi_id::bigint as id \
            , '<b>'||kurzbezeichnung||'</b>' as label \
            , 'layer' as origin \
            , volltextsuche as detail \
            , bod_layer_id as layer \
            , 'it' as lang \
            , projekte as topics \
            , bgdi_id::bigint as id \
        from view_bod_layer_info_it
    sql_attr_bigint = id
    sql_attr_string = lang
    sql_attr_string = origin
    sql_attr_string = label
    sql_field_string = topics
    sql_field_string = layer
    sql_field_string = detail
}

source src_layers_rm
{
    type = pgsql
    sql_host = $1
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = bod
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT \
            bgdi_id::bigint as id \
            , '<b>'||kurzbezeichnung||'</b>' as label \
            , 'layer' as origin \
            , volltextsuche as detail \
            , bod_layer_id as layer \
            , 'rm' as lang \
            , projekte as topics \
            , bgdi_id::bigint as id \
        from view_bod_layer_info_rm
    sql_attr_bigint = id
    sql_attr_string = lang
    sql_attr_string = origin
    sql_attr_string = label
    sql_field_string = topics
    sql_field_string = layer
    sql_field_string = detail
}

source src_ch_astra_ivs-nat
{
    type = pgsql
    # pgcluster0t
    sql_host = $1
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = uvek
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8' 
    sql_query = \
        SELECT \
            nat_id::bigint as id \
            , ivs_slaname as label \
            , remove_accents(ivs_slaname)||' '||remove_accents(ivs_nummer)||' '||remove_accents(ivs_signatur) as detail \
            , 'ch.astra.ivs-nat' as layer \
            , quadindex(the_geom) as geom_quadindex \
            , st_box2d(the_geom) as geom_st_box2d   \
            , nat_id::bigint as id \
        from astra.ivs_nat
    sql_attr_bigint = id
    sql_attr_string = layer
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail 
}

source src_ch_astra_ivs-reg_loc
{
    type = pgsql
    sql_host = $1
    sql_user = $PGUSER
    sql_pass = $PGPASS
    sql_db = uvek
    sql_port = 5432
    sql_query_pre = SET NAMES 'UTF8'
    sql_query = \
        SELECT reg_loc_id::bigint as id \
            , ivs_slaname as label \
            , remove_accents(ivs_slaname)||' '||remove_accents(ivs_nummer)||' '||remove_accents(ivs_signatur) as detail \
            , 'ch.astra.ivs-reg_loc' as layer \
            , quadindex(the_geom) as geom_quadindex \
            , st_box2d(the_geom) as geom_st_box2d \
            , reg_loc_id::bigint as id \
        from astra.ivs_reg_loc
    sql_attr_bigint = id
    sql_attr_string = layer
    sql_attr_string = geom_st_box2d
    sql_field_string = geom_quadindex
    sql_field_string = detail
}

index zipcode
{
    type = plain
    source = src_zipcode
    path = /var/sig/shp/sphinx/data/zipcode
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index district
{
    type = plain
    source = src_district
    path = /var/sig/shp/sphinx/data/district
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index kantone
{
    type = plain
    source = src_kantone
    path = /var/sig/shp/sphinx/data/kantone
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index gg25
{
    type = plain
    source = src_gg25
    path = /var/sig/shp/sphinx/data/gg25
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index sn25
{
    type = plain
    source = src_sn25
    path = /var/sig/shp/sphinx/data/sn25
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index parcel
{
    type = plain
    source = src_parcel
    path = /var/sig/shp/sphinx/data/parcel
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index address
{
    type = plain
    source = src_address
    path = /var/sig/shp/sphinx/data/address
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 2
    infix_fields = detail, geom_quadindex
    enable_star = 1
}

index layers_de
{
    type = plain
    source = src_layers_de
    path = /var/sig/shp/sphinx/data/layers_de
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3
    infix_fields = detail, layer, topics
}

index layers_fr
{
    type = plain
    source = src_layers_fr
    path = /var/sig/shp/sphinx/data/layers_fr
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3
    infix_fields = detail, layer, topics
}

index layers_en
{
    type = plain
    source = src_layers_en
    path = /var/sig/shp/sphinx/data/layers_en
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3
    infix_fields = detail, layer, topics
}

index layers_it
{
    type = plain
    source = src_layers_it
    path = /var/sig/shp/sphinx/data/layers_it
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3
    infix_fields = detail, layer, topics
}

index layers_rm
{
    type = plain
    source = src_layers_rm
    path = /var/sig/shp/sphinx/data/layers_rm
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3
    infix_fields = detail, layer, topics
}

index ch_astra_ivs-nat
{
    type = plain # Which is also the default value
    source = src_ch_astra_ivs-nat
    path = /var/sig/shp/sphinx/data/ch_astra_ivs-nat
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3 # At least 3 letters within a word
    infix_fields = detail
}

index ch_astra_ivs-reg_loc
{
    type = plain # Which is also the default value
    source = src_ch_astra_ivs-reg_loc
    path = /var/sig/shp/sphinx/data/ch_astra_ivs-reg_loc
    docinfo = extern
    charset_type = utf-8
    min_infix_len = 3 # At least 3 letters within a word
    infix_fields = detail
}

indexer
{
    mem_limit = 254M
    max_iosize = 1048576
}

searchd
{
    port = 3312
    log = /var/sig/shp/sphinx/log/searchd.log
    query_log = /var/sig/shp/sphinx/log/query.log
    read_timeout = 5
    max_children = 30
    pid_file = /var/sig/shp/sphinx/log/searchd.pid
    max_matches = 10000
}
"
